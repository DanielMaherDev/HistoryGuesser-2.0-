import React from 'react';
import './Header.css';

const Header = () => {
    return ( <
        div className = "header" >
        <
        span className = "timer" > 00: 59 < /span> <
        /div>
    );
};

export default Header;